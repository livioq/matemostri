# Momo illustrated stages

Add transparent WebP production art using the filenames in `stages.json`, then set the matching `assetReady` value in `index.html` to `true`. Until then, the game uses its lightweight legacy SVG fallback. Locked journey cards never request these files.
